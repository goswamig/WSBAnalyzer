version: '3.8'

services:
  wsb-analyzer:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: wsb-analyzer
    volumes:
      - ./logs:/var/log
    env_file:
      - .env
    restart: unless-stopped